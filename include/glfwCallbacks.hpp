#ifndef H_GLFW_CALLBACKS
#define H_GLFW_CALLBACKS
#include "glad/glad.h"
#include <GLFW/glfw3.h>

namespace callbacks {
    void framebufferSize(GLFWwindow* window, int width, int height);
}

#endif // H_GLFW_CALLBACKS
